query globalSearch($keyWord: String!){
    globalSearch(query: $keyWord)
    {
        id
        title
        content
        view
        like
        comments_num
        position
        tag
    }
}